
1 config.htmlを編集し、{}の部分を置き換え、どこかのWebサイトに配置する（将来的には自動的にfrontの直下に自動的に入れる）。
　config.htmlのファイル名は変更可。そのURLをmanifest.jsonのconfigurationUrlにセットする。

2 manifest.jsonを編集し、{}の部分を置き換える。

3 必要に応じて、color.png, outline.pngを同じサイズで別の中身に差し替えても可

4 manifest.json, color.png, outline.pngをzip化する。

5 Microsoft Teamsにカスタムアプリとしてアップロードする。
